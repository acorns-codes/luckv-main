<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.luckv.demo.mapper.VideoMapper">

	<!-- 동영상등록 -->
	<insert id="videoUpload"  parameterType="com.luckv.demo.dto.Video">
        insert into 
        	video (
        		ano, /*경매 게시글 번호*/
        		video_file /*동영상 이름*/
	        ) values (
	        	#{ano},
	        	#{videoFile}
	        )
    </insert>
	
	<!--QnA 조회 -->
    <select id="videoDownload" parameterType="Integer" resultType="com.luckv.demo.dto.Video">
        select 
        	ano, /*경매 게시글 번호*/
        	video_file /*동영상 이름*/
        from video 
        	where ano = #{ano}
    </select>
    
</mapper>